#!/usr/bin/env python
# encoding: utf-8

#带通配符的字符串匹配问题
"""
字符串匹配问题,给定一串字符串,按照制定规则对其进行匹配,并将匹配结果保存至
output数组中,多个匹配项用空格间隔,最后一个不需要空格.

1. 匹配规则中包含通配符?和\*，其中?表示匹配任意一个字符，\*表示匹配任意多个（>=0）字符。
2. 匹配规则要求匹配最大的字符子串，例如a\*d,匹配abbdd而非abbd,即最大匹配子串。
3. 匹配后的输入串不再进行匹配，从当前匹配后的字符串重新匹配其他字符串。

"""
